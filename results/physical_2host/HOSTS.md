# Physical two-host campaign

Two LAN hosts, 10 processes each (p=20 total).
Hardware class: laptop-class x86_64, 6 cores / 12 threads, ~20 GB RAM, Windows 11.
Network: same LAN (Wi-Fi). Exact hostnames and IP addresses omitted from the public artifact.
See `summary.json` and run logs for agreement statistics (zero mismatches).
